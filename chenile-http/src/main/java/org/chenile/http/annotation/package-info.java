/**
 * Extended Chenile HTTP based annotations.
 * These annotations are used at the controller class to inform Chenile about specific metadata.
 * Note that most of the info required by Chenile is already supplied by Spring annotations. Chenile uses
 * them to initialize itself. This is additional metadata.
 */
package org.chenile.http.annotation;